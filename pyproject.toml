[tool.black]
line-length = 100
target-version = ["py311"]
exclude = """
/(
  | migrations
  | staticfiles
  | .venv
  | venv
)/
"""

[tool.isort]
profile = "black"
line_length = 100
skip_glob = ["*/migrations/*"]
